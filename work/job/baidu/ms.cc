#include <iostream>

using namespace std;

int main(void) {
    int a, b;
    /* while(cin >> a >> b) {
        cout << a + b << endl;
    } */

    /* while(scanf("%d%d", &a, &b) != EOF) {
        printf("%d\n", a + b); */
    int i  = 3;
    cout << ((1 << i) -2 ) << endl;
    return 0;
}
