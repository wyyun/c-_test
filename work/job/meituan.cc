#include <stdlib.h>
#include <stdio.h>
#include <string.h>

char* formatstring(char *source)
{
}

int main()
{
    char *source = "   i love  meituan   ";
    /* char *meituan = formatstring(source);
    printf("%s\n",meituan); */
    char *s = "12345";
    printf("%s : %d\n",s,strlen(s));
}
